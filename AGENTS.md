# AGENTS.md

This repository is a lightweight GitHub Pages mirror for TheCAVE AI class materials.

## Source Of Truth

- Do not treat this repository as the source project.
- The source project is `/Users/minhokim/projects/local-ax-playbook`.
- The current Chiljeonpalpal source slide file is `/Users/minhokim/projects/local-ax-playbook/docs/courses/chiljeon-palpal-school/slides.html`.
- This repository exists only to publish student-facing static material links.

## Editing Rules

- Do not edit slide content here unless the user explicitly asks to patch the preview mirror.
- Make real content changes in the source project, then copy the changed preview files into this repository.
- Keep this repository small: root `index.html`, course `index.html` mirrors, required `assets/`, `README.md`, `AGENTS.md`, and `.nojekyll`.
- The root `index.html` is a poster-card portal with lightweight course-code entry. It is not real access control.

## Publishing

- GitHub Pages should publish from `main` and `/ (root)`.
- Root `index.html` is the entry file.
- Course decks live under `courses/<course>/<session>/`.
